import { useState } from 'react'
import { Icon } from '../components/Icons'

export function GetStarted() {
  const [form,setForm]=useState({name:'',email:'',portfolio:'',specialty:'',vision:''}); const [sent,setSent]=useState(false); const update=(e)=>setForm({...form,[e.target.name]:e.target.value})
  return <section className="form-page page-shell creator-form"><div className="form-intro"><span className="eyebrow">Creator atelier</span><h1>Bring us what<br/><em>doesn’t exist yet.</em></h1><p>Apply to release your work through FashionXpress. We review every portfolio with a human eye.</p><ul><li><Icon name="check"/> Global marketplace access</li><li><Icon name="check"/> AR and digital sampling tools</li><li><Icon name="check"/> Transparent royalties</li></ul></div>{sent ? <div className="form-success panel"><Icon name="spark" size={38}/><span className="eyebrow">Application received</span><h2>Welcome to the future.</h2><p>Our curators will review your portfolio and contact you within 48 hours.</p></div> : <form className="editorial-form" onSubmit={(e)=>{e.preventDefault();setSent(true)}}><div className="field-pair"><label>Name<input required name="name" value={form.name} onChange={update} placeholder="Full name"/></label><label>Email<input required type="email" name="email" value={form.email} onChange={update} placeholder="Email address"/></label></div><label>Portfolio<input required type="url" name="portfolio" value={form.portfolio} onChange={update} placeholder="https://"/></label><label>Design practice<select required name="specialty" value={form.specialty} onChange={update}><option value="">Select your discipline</option><option>3D streetwear</option><option>Virtual couture</option><option>Digital accessories</option><option>Material artist</option></select></label><label>Your vision<textarea required rows="5" name="vision" value={form.vision} onChange={update} placeholder="What world are you trying to build?"/></label><button className="button" type="submit">Submit application <Icon name="arrow" size={16}/></button></form>}</section>
}
