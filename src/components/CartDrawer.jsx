import { Icon } from './Icons'
export function CartDrawer({ open, items, onClose, onRemove }) {
  const total = items.reduce((sum, item) => sum + item.price, 0)
  return <div className={open ? 'drawer-shell open' : 'drawer-shell'} aria-hidden={!open}><button className="drawer-backdrop" onClick={onClose} aria-label="Close bag"/><aside className="drawer" aria-label="Shopping bag"><div className="drawer-head"><div><span className="eyebrow">Your edit</span><h2>Bag ({items.length})</h2></div><button className="icon-button" onClick={onClose}><Icon name="close"/></button></div><div className="drawer-items">{items.length === 0 ? <div className="empty"><Icon name="bag" size={36}/><h3>Your bag is quiet</h3><p>Collect a piece from the latest digital drop.</p></div> : items.map((item, index) => <div className="cart-row" key={`${item.id}-${index}`}><img src={item.image} alt=""/><div><strong>{item.name}</strong><span>{item.rarity}</span><button onClick={() => onRemove(index)}>Remove</button></div><b>{item.price.toFixed(1)} ETH</b></div>)}</div><div className="drawer-total"><div><span>Total</span><strong>{total.toFixed(1)} ETH</strong></div><button className="button full" disabled={!items.length}>Secure checkout <Icon name="arrow" size={16}/></button><small>Demo checkout · No wallet transaction will occur</small></div></aside></div>
}
